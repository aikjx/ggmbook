# 兼容入口说明

本目录不再承载正式归档脚本，只保留旧路径兼容包装与废弃提示。

正式脚本目录：

- `scripts/archives/`
- `scripts/archives/legacy/`

推荐命令：

- `pnpm run archive:build`
- `pnpm run archive:build:clean`
- `pnpm run archive:rebuild`
- `pnpm run archive:localize-images`
